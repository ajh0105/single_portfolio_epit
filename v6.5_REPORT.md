# ocrv6.4 / ocrv6.5 개발 보고서

작성일: 2026-05-24  
최종 모델: `ocrv6.5/pt/best.pt` (31.8 MB, ep9 BEST)

---

## 1. 개요

| 항목 | 내용 |
|------|------|
| 목표 | ocrv6.3 대비 Rare 정확도 회복 (0.9748 → ≥ 0.99) 및 종합 성능 향상 |
| 아키텍처 | V5OCR (STN → VGG4 → CBAM → BiLSTM(256) → Linear(51)) — v5와 동일 |
| 데이터셋 | 실데이터(ocrv6 dataset2 재분할) + 합성(ohjj/yakhyo) 총 79,642장 |
| 최종 결과 | comp=**0.9980**, hard=**1.0000** (전 카테고리), rare=**0.9952** |

---

## 2. 배경 — ocrv6.3 문제점

ocrv6.3은 comp=0.9918로 목표를 달성했지만 두 가지 핵심 문제가 있었다.

### 2-1. Rare 정확도 회귀
- ocrv5 rare: **1.0000** → ocrv6.3 rare: **0.9748** (−2.52%p)
- 대상 문자: 바·배·사·아·자·허 (학습 데이터 각 163~491장)
- WeightedRandomSampler(2배) + FocalCTC 이중 적용에도 불구하고 부족

### 2-2. ocrv6.1 / 6.2 실패 원인 (재발 방지)
```python
# 버그: 0-indexed (ocrv6.1/6.2)
char2idx = {c: i for i, c in enumerate(chars)}   # "0"→0, 공백과 충돌

# 정상: 1-indexed, blank=0 (ocrv6.3 이후 유지)
char2idx = {c: i + 1 for i, c in enumerate(chars)}  # "0"→1
```
→ 이 규약을 반드시 유지해야 val_acc=0.0000 버그를 피할 수 있다.

---

## 3. ocrv6.4 — 처음부터 학습 (From Scratch)

### 3-1. 데이터셋 구성

```
build_dataset.py 실행 결과:
  실데이터  (ocrv6 dataset2 재분할): ~62,000장
  합성데이터 ohjj  + yakhyo       : ~17,600장
  ──────────────────────────────────────────
  Train: 63,753장  /  Val: 15,889장  (total 79,642)

제외: 지역명 번호판 (경기·인천 등, ~17,767장)
  → PAT7/PAT8 패턴(NN한글NNNN / NNN한글NNNN)에 불일치
  → Grammar decoder와 num_classes 변경 없이 수용 불가
```

### 3-2. Hard Val Set 구성

```
hard_split.py → artifacts/splits/hard_dev_indices.json (250장)

카테고리          수량   조건
small_plate  :   63장   w ≤ 80px 또는 h ≤ 32px
motion_blur  :   62장   Laplacian variance < 200
night        :   50장   luma < 80
crop_cut     :   38장   가장자리 fg 비율 > 0.25
skew         :   25장   Hough line 기반 기울기 > 5°
rare         :   12장   바·배·사·아·자·허 포함
```

### 3-3. 주요 하이퍼파라미터

| 파라미터 | 값 | 비고 |
|----------|-----|------|
| LR | 1e-3 | AdamW + warmup(5ep) + CosineAnnealing |
| BATCH | 96 | |
| RARE_REWEIGHT | 3.0 | ocrv6.3의 2.0 → 상향 |
| EMA_START_EP | 10 | epoch 10부터 EMA 적용 |
| EMA_DECAY | 0.999 | |
| GRAD_CLIP | 5.0 | |
| PATIENCE | 10 | |
| PATIENCE_START | 20 | epoch 20 이전 early-stop 금지 |
| MAX_EPOCHS | 100 | |

### 3-4. 학습 결과

```
[ep   6/100|p1] val=0.9852  hard=0.964  rare=0.9904  comp=0.9762  (BEST)
[ep   7/100|p1] val=0.9918  hard=0.980  rare=0.9880  comp=0.9848  (BEST)
[ep   8/100|p1] val=0.9892  hard=0.988  rare=0.9904  comp=0.9890  (BEST)
[ep  20/100|p1] val=0.9902  hard=0.988  rare=0.9809  comp=0.9863
[early-stop]    ep=20  no_improve=13 >= patience=10
```

**BEST: ep8** — comp=0.9890, val=0.9892, hard=0.9880, rare=0.9904, ece=0.0061  
Hard 카테고리: S=1.000, **B=0.952**, N=1.000, C=1.000, K=1.000, R=1.000

**문제**: ep10에서 EMA 시작 이후 rare가 0.9904 → 0.9809로 급락 후 고착

---

## 4. ocrv6.5 — 파인튜닝 (Fine-tuning)

### 4-1. 파인튜닝 결정 근거

ocrv6.4 ep8이 rare=0.9904로 목표에 근접했고, EMA 시작 충격이 명확한 회귀 원인이었으므로 처음부터 재학습보다 파인튜닝이 유리하다고 판단.

| 비교 항목 | 처음부터 학습 | **파인튜닝 선택** |
|----------|------------|--------------|
| 출발점 | 랜덤 | ocrv6.4 ep8 (rare=0.9904) |
| 수렴 시간 | 8+ epoch | 1~3 epoch |
| EMA 문제 재발 위험 | 높음 | 낮음 (LR 낮아 안정) |
| ocrv6.3 선례 | — | ep1~2 comp 0.9918 달성 (검증됨) |

### 4-2. 주요 변경 사항 (ocrv6.4 대비)

| 파라미터 | ocrv6.4 | ocrv6.5 | 변경 이유 |
|----------|---------|---------|----------|
| 시작점 | 랜덤 초기화 | ocrv6.4 ep8 best.pt | 이미 수렴한 가중치 재사용 |
| LR | 1e-3 | **5e-5** | 파인튜닝 안정화, ocrv6.3 검증값 |
| LR Schedule | warmup+cosine | **CosineAnnealing만** | warmup 불필요 |
| EMA | ep10부터 decay=0.999 | **제거** | EMA가 지표 고착 원인 |
| RARE_REWEIGHT | 3.0 | **4.0** | rare 강화 |
| Augmentation | p1→p2→p3 커리큘럼 | **고정 p2** | 파인튜닝: 커리큘럼 불필요 |
| GRAD_CLIP | 5.0 | **2.0** | 파인튜닝: tight clip |
| PATIENCE | 10 | **15** | 충분한 인내 |
| PATIENCE_START | 20 | **5** | 파인튜닝: 빠른 수렴 반영 |
| MAX_EPOCHS | 100 | **50** | 파인튜닝: 빠른 수렴 |

### 4-3. 최종 학습 결과

```
[ep   1/50] val=0.9959  hard=0.996  rare=0.9952  comp=0.9957  (BEST)  [S=1.000 B=0.984 N=1.000 C=1.000 K=1.000 R=1.000]
[ep   3/50] val=0.9969  hard=0.996  rare=0.9952  comp=0.9959  (BEST)  [S=1.000 B=0.984 N=1.000 C=1.000 K=1.000 R=1.000]
[ep   9/50] val=0.9972  hard=1.000  rare=0.9952  comp=0.9980  (BEST)  [S=1.000 B=1.000 N=1.000 C=1.000 K=1.000 R=1.000]
[ep  24/50] early-stop  no_improve=15 >= patience=15
```

**BEST: ep9** — comp=**0.9980**, val=0.9972, hard=**1.0000**, rare=0.9952, ece=0.0112  
Hard 카테고리: **S=1.000 B=1.000 N=1.000 C=1.000 K=1.000 R=1.000 (전 카테고리 완벽)**

---

## 5. 최종 성능 비교

| 지표 | v5 | ocrv6.3 | ocrv6.4 | **ocrv6.5** | 목표 |
|------|-----|---------|---------|------------|------|
| Val | 0.9870 | 0.9970 | 0.9892 | **0.9972** | ≥0.9970 |
| Hard | ~0.9800 | 1.0000 | 0.9880 | **1.0000** | 1.0000 유지 |
| ↳ B (블러) | — | — | 0.9516 | **1.0000** | — |
| Rare | **1.0000** | 0.9748 | 0.9904 | **0.9952** | ≥0.9900 ✓ |
| Conf | 0.9924 | 0.9966 | 0.9880 | **0.9970** | ≥0.9966 |
| ECE↓ | 0.0052 | 0.0041 | 0.0061 | 0.0112 | — |
| **Comp** | 0.9874 | 0.9918 | 0.9890 | **0.9980** | ≥0.9930 ✓ |

> v5 Hard는 746장 기준, ocrv6 계열은 250장 기준 (직접 비교 시 주의)

---

## 6. 트러블슈팅 기록

### TS-1: val=0.0000 (ep1~2) — 정상 현상

**증상**: ep1, ep2 에서 val=0.0000으로 표시됨  
**원인**: CTC 수렴 전 정상 동작. 처음부터 학습 시 ep3~5에서 수렴 시작  
**해결**: 기다리면 됨. ep3에서 val=0.61, ep5에서 val=0.95로 급격히 수렴

---

### TS-2: ep10 early-stop (ocrv6.4 1차 시도)

**증상**: `[early-stop] ep=10  no_improve=10 >= patience=10`  
**원인 1**: `best_comp = 0.0` 초기값 → ep1의 comp=0.0이 `> 0.0` 조건을 만족하지 않아 BEST가 아님 → no_improve가 ep1부터 누적  
**원인 2**: `NUM_WORKERS=4`로 Windows DataLoader 불안정

```python
# 수정 전
best_comp = 0.0         # → comp=0.0 > 0.0 → False
NUM_WORKERS = 4

# 수정 후
best_comp = -1.0        # ep1은 반드시 BEST
NUM_WORKERS = 0         # Windows spawn 이슈 방지
```

**추가 수정**: `PATIENCE_START = 20` 추가 (epoch 20 이전 early-stop 금지)

---

### TS-3: ep20 early-stop (ocrv6.4 2차 시도)

**증상**: `[early-stop] ep=20  no_improve=13 >= patience=10`  
**원인**: EMA가 ep10에 deepcopy로 시작되면서 rare가 0.9904 → 0.9809로 급락. 이후 no_improve가 계속 누적.  

```
ep  8: rare=0.9904  (BEST, raw model)
ep 10: EMA 시작 → rare=0.9808 (EMA model, 급락)
ep 11~20: rare=0.9809 고착 → no_improve 계속 증가
```

**해결**: ocrv6.5로 전환. LR=5e-5 파인튜닝 + EMA 제거

---

### TS-4: ocrv6.5 지표 고착 (EMA_DECAY=0.9995 문제)

**증상**: ep1~6 전 지표가 완전히 동일 (val=0.9892, rare=0.9904 고정)  
**원인**: EMA_DECAY=0.9995로 ep6까지의 새 학습 반영률:
```
1 - 0.9995^6 ≈ 0.3%  → EMA 모델의 99.7%가 여전히 ocrv6.4 ep8 가중치
```
**해결**: EMA 완전 제거 → `eval_model = model` (raw model 직접 평가)

```python
# 수정 전
ema = ModelEMA(model, decay=0.9995)
ema.update(model)
eval_model = ema.module

# 수정 후 (EMA 제거)
eval_model = model  # LR=5e-5 파인튜닝은 안정적이라 EMA 불필요
```

---

### TS-5: RNN contiguous memory warning

**증상**: `UserWarning: RNN module weights are not part of single contiguous chunk of memory`  
**원인**: `copy.deepcopy(model)` 수행 시 LSTM weight의 메모리 연속성이 깨짐  
**영향**: 성능 경고만, 학습/추론 결과에는 영향 없음  
**해결**: 무시 (EMA 제거 후 자동으로 사라짐)

---

### TS-6: cmd.exe에서 `&` 명령어 오류

**증상**: `& "C:\cuda\cuda\Scripts\python.exe" ...` 실행 시 오류  
**원인**: `&`는 PowerShell 전용 호출 연산자. cmd.exe에서는 미지원  
**해결**: cmd.exe에서는 `&` 없이 직접 실행
```cmd
"C:\cuda\cuda\Scripts\python.exe" scripts/train.py
```

---

## 7. ocrv6.5 사용 방법

### 7-1. 환경

```
Python  : C:\cuda\cuda\Scripts\python.exe
PyTorch : 2.5.1+cu121
CUDA    : RTX 4060 Laptop
모델    : C:\jh\team_hifive\260521_ocr_project\ocrv6.5\pt\best.pt
```

### 7-2. 빠른 추론 (권장)

```python
import sys, cv2, json
import numpy as np
import torch
import torch.nn.functional as F

# 경로 설정
PROJ    = r"C:\jh\team_hifive\260521_ocr_project"
CKPT    = rf"{PROJ}\ocrv6.5\pt\best.pt"
VOCAB   = rf"{PROJ}\ocrv6.5\artifacts\vocab.json"

# V5OCR 모델 로드
sys.path.insert(0, PROJ)
from ocrv5.code.model import V5OCR

with open(VOCAB, encoding="utf-8") as f:
    v = json.load(f)
chars    = v["chars"]
idx2char = {i + 1: c for i, c in enumerate(chars)}
idx2char[0] = ""
num_classes  = v["num_classes"]  # 51

device = torch.device("cuda" if torch.cuda.is_available() else "cpu")
model  = V5OCR(num_classes=num_classes).to(device)

ckpt  = torch.load(CKPT, map_location=device, weights_only=False)
model.load_state_dict(ckpt["model"])
model.eval()
print(f"ocrv6.5 loaded  ep={ckpt['epoch']}  comp={ckpt['comp']:.4f}")


def preprocess(img_bgr: np.ndarray) -> torch.Tensor:
    """번호판 crop 이미지(BGR) → 모델 입력 텐서"""
    img = cv2.resize(img_bgr, (160, 48), interpolation=cv2.INTER_LINEAR)
    img = cv2.cvtColor(img, cv2.COLOR_BGR2RGB).astype(np.float32) / 255.0
    img = (img - 0.5) / 0.5
    return torch.from_numpy(img.transpose(2, 0, 1))  # (3, 48, 160)


def ctc_greedy(log_probs: torch.Tensor) -> str:
    """CTC greedy decode → 번호판 문자열"""
    seq, prev = [], -1
    for v in log_probs.argmax(-1).cpu().tolist():
        if v != prev and v != 0:
            seq.append(idx2char.get(v, ""))
        prev = v
    return "".join(seq)


def predict(img_bgr: np.ndarray) -> str:
    """단일 이미지 추론"""
    x  = preprocess(img_bgr).unsqueeze(0).to(device)  # (1,3,48,160)
    with torch.no_grad():
        lp = F.log_softmax(model(x).permute(1, 0, 2), dim=-1)  # (T,1,51)
    return ctc_greedy(lp[:, 0, :])


# 사용 예시
img    = cv2.imread("plate_crop.jpg")
result = predict(img)
print(result)  # "01가1234"
```

### 7-3. 배치 추론

```python
def predict_batch(imgs: list) -> list:
    """여러 이미지 동시 추론 (빠름)"""
    tensors = torch.stack([preprocess(img) for img in imgs]).to(device)
    with torch.no_grad():
        lp = F.log_softmax(model(tensors).permute(1, 0, 2), dim=-1)  # (T,B,51)
    return [ctc_greedy(lp[:, i, :]) for i in range(len(imgs))]

# 예시
paths   = ["plate1.jpg", "plate2.jpg", "plate3.jpg"]
imgs    = [cv2.imread(p) for p in paths]
results = predict_batch(imgs)
for p, r in zip(paths, results):
    print(f"{p}: {r}")
```

### 7-4. YOLO + ocrv6.5 파이프라인

```python
# YOLO로 번호판 감지 → ocrv6.5로 인식하는 전체 파이프라인 예시
from ultralytics import YOLO

det_model = YOLO("yolo_lp_detect.pt")  # 번호판 탐지 모델

def pipeline(frame_bgr: np.ndarray) -> list:
    """
    입력: 전체 프레임 (BGR)
    출력: [(번호판 텍스트, bbox), ...] 리스트
    """
    results = det_model(frame_bgr, conf=0.5)
    plates  = []
    for box in results[0].boxes.xyxy.cpu().numpy():
        x1, y1, x2, y2 = map(int, box)
        crop = frame_bgr[y1:y2, x1:x2]
        if crop.size == 0:
            continue
        text = predict(crop)
        plates.append((text, (x1, y1, x2, y2)))
    return plates

# 비디오 처리 예시
cap = cv2.VideoCapture("highway.mp4")
while cap.isOpened():
    ret, frame = cap.read()
    if not ret:
        break
    detections = pipeline(frame)
    for text, (x1, y1, x2, y2) in detections:
        cv2.rectangle(frame, (x1, y1), (x2, y2), (0, 255, 0), 2)
        cv2.putText(frame, text, (x1, y1 - 10),
                    cv2.FONT_HERSHEY_SIMPLEX, 0.9, (0, 255, 0), 2)
    cv2.imshow("result", frame)
    if cv2.waitKey(1) & 0xFF == ord("q"):
        break
cap.release()
```

### 7-5. 체크포인트 키 구조

```python
ck = torch.load("ocrv6.5/pt/best.pt", weights_only=False)
# {
#   "epoch"    : 9,
#   "model"    : OrderedDict(...),   ← V5OCR state_dict (raw model, EMA 없음)
#   "val_acc"  : 0.9972,
#   "hard_acc" : 1.0000,
#   "rare_acc" : 0.9952,
#   "conf_acc" : 0.9970,
#   "ece"      : 0.0112,
#   "comp"     : 0.9980,
# }
```

### 7-6. 학습 재실행 방법

```cmd
cd C:\jh\team_hifive\260521_ocr_project\ocrv6.5
"C:\cuda\cuda\Scripts\python.exe" scripts/train.py
```

옵션:
```cmd
# 빠른 동작 확인 (2 epoch)
"C:\cuda\cuda\Scripts\python.exe" scripts/train.py --debug

# 다른 seed로 실행
"C:\cuda\cuda\Scripts\python.exe" scripts/train.py --seed 3407

# 최대 epoch 지정
"C:\cuda\cuda\Scripts\python.exe" scripts/train.py --epochs 30
```

---

## 8. 파일 위치

```
260521_ocr_project/
├── ocrv6.4/
│   ├── pt/
│   │   ├── best.pt              ocrv6.4 BEST (ep8, comp=0.9890)
│   │   └── last.pt              ocrv6.4 마지막 epoch
│   ├── dataset/
│   │   ├── train/images/        63,753장
│   │   └── val/images/          15,889장
│   ├── artifacts/
│   │   ├── vocab.json           50자 vocab (num_classes=51)
│   │   ├── rare_chars.json      rare/confusable 정의
│   │   └── splits/
│   │       └── hard_dev_indices.json  hard val 250장
│   ├── scripts/
│   │   ├── build_dataset.py     데이터셋 구성 스크립트
│   │   ├── hard_split.py        hard val set 생성 스크립트
│   │   └── train.py             ocrv6.4 학습 스크립트 (from scratch)
│   └── logs/
│       ├── train.csv            epoch별 지표 CSV
│       └── metrics.json         epoch별 지표 JSON
│
└── ocrv6.5/
    ├── pt/
    │   ├── best.pt              ★ 최종 모델 (ep9, comp=0.9980)
    │   └── last.pt              마지막 epoch
    ├── artifacts/               (ocrv6.4에서 복사)
    │   ├── vocab.json
    │   ├── rare_chars.json
    │   └── splits/hard_dev_indices.json
    ├── scripts/
    │   └── train.py             ocrv6.5 파인튜닝 스크립트
    ├── logs/
    │   ├── train.csv
    │   └── metrics.json
    └── REPORT.md                ← 이 파일
```

---

## 9. 향후 과제

| 항목 | 상태 | 설명 |
|------|------|------|
| 3-run 재현성 검증 | 미완료 | seed 42/3407/2026으로 3회 학습, mean hard ≥ 0.987 확인 |
| ONNX 변환 | 미완료 | best.pt → ONNX (opset 17, dynamic batch) |
| TensorRT 배포 | 미완료 | ONNX → TensorRT 엔진 변환 |
| ohjj 도메인 검증 | 미완료 | 고속도로 CCTV 특화 데이터셋 별도 평가 |
| 지역명 번호판 처리 | 미완료 | detection 단계 분기 후 별도 OCR 모델 개발 예정 |
